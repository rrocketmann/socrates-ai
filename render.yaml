services:
  - type: web
    name: socrates-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -b 0.0.0.0:10000 app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    plan: free
